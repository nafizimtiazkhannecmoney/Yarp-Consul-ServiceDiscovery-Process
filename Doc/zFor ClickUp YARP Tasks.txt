1. Implemented Yarp to Gateway service(Yarp.ReverseProxy 2.3.0)
2. Two services is being added to gateway yarps cluster
3. Interceptor is being added to Gateway service progra.cs to monitor service execution
4. Moved the yarp configuration from appsettings to program memory which the program LoadFrom Memory
5. Caching is added in proxypipeline so every request is cached and the second consecuting request is faster
6. Consul(1.7.14.7) Is added to ApiGateway Service (Caching config is managed from memory in MemoryCacheService.cs) 
7. User System and user Policy is managed from YARP settings
8. Added IdentityDbContext to manage users (Users are stored in Sqlight temporarily)
9. Added IdentityController, which imlements Authentication, Login, Register. And Also generates a JWT token to a logged in user.
10. Both of the other service which will go through gate way is configured in YARP BasicConfiguration.cs and the destinations are hard coded.
11. Both of the services are registered to consul through program.cs by AgentServiceRegistration(), consul reads the services from here.
12. Configured the launchsettings of gateway and service A and service B to rout through the gateway efficiently.
13. Authentication system is configured from the gateway service
14. Configured user role from YARP Settings
15. Registered apigateway 
16. Both of the service has their own ConsulRegistration.cs Config now(Consul 1.7.14.7 is installed in both), so the request URL is dynamic now, and the apigateway is also registered to the consul terminal for better monitor of services.
17. Health check added for all services on the consul
18. Added goAML and SARB service on the gateway in the place of ServiceA and ServiceB
19. Cleaned all the three projects which had existing old configurations to adapt to the new structure.
20. Added GatewayConsulRegistration.cs for the gateway to be self registered to the consul, and added console log
21. Further configured the JWT settings from the apigateway service.
22. Removed the infrastructure part from the gateway(DbContext and Auth)
23. Userservice now handles the users, it contains DbContext, Added file Logger. and the loggs will be printed outside of the project
24. Inthe UserService implemented Repository that connects with postgre database and calls the Stored Procedures.
and implements GET_ALL_USER, GET_USER_BY_ID, SIGN_IN by the stored procedure. and the claim and role info is inserted to the JWT token.
25. Added health checks to endpoints
26. Added RoleClaimMiddleware so that the the user is verified from the middleware so suspicious userrs are checked and blocked from the gateway and never goes to the user service for better security. Also the token is being validated from here. Serilog is configured in appsettings.
27. ADD_USER endpoint implemented which adds user by ADD_USER action from the stored procedure in the prostgre DB.
28. UPDATE_USER Implemented
29. ModId is configured in a way that the user who updated the info its user id is reflected and added the the database in modid property.
30. ChangePassword endpoint added which updates password using UPD_PWD action from the postgre stored procedure
31. Created SEL_use SP which Returns the User information.
32. Created Table Scripts for SQL Server - GenericMap, GenericMapAudit, Group, GroupAudit, Role, RoleAudit, User, UserAudit.
33. Created Triggers for these tables for auditing(which works for both insert and update action) - TR_User, trg_GenericMap_Audit, trg_Group_Audit, trg_Role_Audit.
34. SQL tables are fixed which matches exactly with postgre db tables
35. Connected the program with sql server and poswtgre simultaneously
36. TransactionHandleService added to simultaneously passs data from program to sql and postgre
37. By changing function parameter the program can fetch data from different type of database



------------------------------------------------------------
Refined ClickUp Task List

Implemented YARP Reverse Proxy (v2.3.0) into the API Gateway service.

Added two backend services to YARP’s cluster configuration.

Introduced an interceptor in Program.cs to monitor incoming requests and routing behavior.

Migrated YARP configuration from appsettings.json to in-memory configuration using LoadFromMemory.

Implemented response caching in the proxy pipeline so repeated requests are served faster.

Integrated Consul (v1.7.14.7) with the API Gateway; caching configuration is handled via MemoryCacheService.cs.

Configured User System and User Policies through YARP settings.

Added IdentityDbContext to manage users (temporary storage in SQLite).

Created IdentityController for authentication (Login, Register) and JWT token generation for authenticated users.

Configured the backend services in BasicConfiguration.cs with hard-coded destinations.

Registered both services to Consul using AgentServiceRegistration() in Program.cs.

Configured launchSettings.json for the Gateway, Service A, and Service B to ensure requests route efficiently through the Gateway.

Implemented authentication configuration within the API Gateway service.

Added user role configuration via YARP settings.

Registered the API Gateway as a service in the solution.

Added ConsulRegistration.cs in each service; now services register dynamically with Consul, and the API Gateway is also registered for monitoring.

Implemented health checks for all services within Consul.

Added goAML and SARB services to the Gateway in place of Service A and Service B.

Cleaned up all three projects, removing legacy configurations to align with the new architecture.

Added GatewayConsulRegistration.cs for the Gateway’s self-registration to Consul, with logging for monitoring.

Extended JWT configuration in the API Gateway service.

Removed infrastructure components (DbContext and Auth) from the Gateway.

Updated UserService to handle user management (DbContext included). Added file logging to write logs outside the project directory.

Implemented a Repository pattern in UserService connecting to PostgreSQL. Repository calls stored procedures:

SEL_USER_ALL (Get all users)

SEL_USER_BY_ID (Get user by ID)

SIGN_IN (User login)
Claims and roles are now included in JWT tokens.

Added health check endpoints for service monitoring.

Implemented RoleClaimMiddleware to validate users at the Gateway layer:

Suspicious users blocked before reaching UserService.

JWT token validation included.

Configured Serilog in appsettings.json.

Implemented ADD_USER endpoint using the ADD_USER stored procedure in PostgreSQL.

Implemented UPDATE_USER endpoint for updating user data.

Configured ModId tracking: whenever a user updates data, their UserId is stored in the ModId field.

Implemented ChangePassword endpoint using the UPD_PWD stored procedure in PostgreSQL.

Created stored procedure SEL_USER to return user details.

Created SQL Server table scripts: GenericMap, GenericMapAudit, Group, GroupAudit, Role, RoleAudit, User, UserAudit.

Added audit triggers (for insert & update): TR_User, trg_GenericMap_Audit, trg_Group_Audit, trg_Role_Audit.

Standardized SQL Server tables to match PostgreSQL schema.

Connected the application with both SQL Server and PostgreSQL simultaneously.

Implemented TransactionHandleService to pass data between SQL Server and PostgreSQL in sync.

Enabled multi-DB support by parameterizing functions so they can fetch data from different databases.












